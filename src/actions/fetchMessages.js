export const fetchMessages = user => {
  // return (dispatch) => {
  //   fetch(`http://localhost:3000/api/v1/users/${user}/messages`)
  //     .then(res => res.json())
  //     .then(messages => console.log(messages))
  // }
}
